# jest-tutorial



## install

# already installed
# yarn add -D 'eslint'

yarn add -D \
  'eslint-config-airbnb-base' \
  'eslint-plugin-import'

yarn add -D 'typescript' '@types/node'

## run

npx eslint src
npx eslint src --fix